{
  "rules": {
    "netatmo" : {
      ".read": "auth != null",
      ".write": "auth.provider === 'password'"
    },
   	"steca" : {
      ".read": "true",
      ".write": "auth.provider === 'password'"
    },
    "settings": {
      ".read": "auth.provider === 'password'",
      ".write": "auth.provider === 'password'"
    },
    "tibber" : {
      ".read": "auth.provider === 'password'",
      ".write": "auth.provider === 'password'"
    }
  }
}