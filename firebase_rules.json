{
  "rules": {
    "netatmo" : {
      ".read": "auth != null",
      ".write": "auth.provider === 'password'"
    },
   	"steca" : {
      ".read": "auth != null",
      ".write": "auth.provider === 'password'"
    }
  }
}